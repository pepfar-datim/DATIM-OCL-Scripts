CREATE TABLE "Datasets" (
	datasetid DECIMAL NOT NULL, 
	name VARCHAR(57) NOT NULL, 
	shortname VARCHAR(48) NOT NULL, 
	code VARCHAR(47) NOT NULL, 
	periodtypeid DECIMAL NOT NULL, 
	dataentryform DECIMAL, 
	mobile BOOLEAN NOT NULL, 
	version DECIMAL NOT NULL, 
	uid VARCHAR(11) NOT NULL, 
	lastupdated DATETIME NOT NULL, 
	expirydays DECIMAL NOT NULL, 
	description VARCHAR(324), 
	notificationrecipients BOOLEAN, 
	fieldcombinationrequired BOOLEAN NOT NULL, 
	validcompleteonly BOOLEAN NOT NULL, 
	skipoffline BOOLEAN NOT NULL, 
	notifycompletinguser BOOLEAN NOT NULL, 
	created DATETIME NOT NULL, 
	userid DECIMAL NOT NULL, 
	publicaccess VARCHAR(8) NOT NULL, 
	timelydays DECIMAL NOT NULL, 
	dataelementdecoration BOOLEAN NOT NULL, 
	renderastabs BOOLEAN NOT NULL, 
	renderhorizontally BOOLEAN NOT NULL, 
	categorycomboid DECIMAL NOT NULL, 
	novaluerequirescomment BOOLEAN NOT NULL, 
	openfutureperiods DECIMAL NOT NULL, 
	workflowid DECIMAL, 
	lastupdatedby BOOLEAN
);
